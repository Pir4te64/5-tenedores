import { StyleSheet } from "react-native";

export const styles = StyleSheet.create({
  img: {
    width: "100%",
    height: 150,
    marginTop: 20,
    resizeMode: "contain",
  },
  container: {
    marginHorizontal: 40,
    marginTop: 20,
  },
});
